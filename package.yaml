name: yesod-minimal
license: MIT

dependencies:
- base
- yesod-core
- yesod-form
- wai
- warp
- text

executables:
  yesod-minimal:
    main: Minimal
    source-dirs: src
    ghc-options:
      - -Wall
      - -O2
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
  yesod-minimal-form:
    main: MinimalForm
    source-dirs: src
    ghc-options:
      - -Wall
      - -O2
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
